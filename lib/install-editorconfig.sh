#! /bin/bash

# Install editorconfig locally.

# For now, a simple hack is to install it via homebrew. If I ever move to
# Linux, I'll have to change this to use the appropriate package manager.
brew install editorconfig
